# Copilot instructions

Apply `AGENTS.md`. Preserve compatibility by default, enforce authorization on the server, add regression tests, and do not invent API, data, infrastructure, or release facts.
